const greeting = "Hello World";

const answer = 'Forty Two';

//Using the `` we can put dynamic values into a string, we just need to use 
//the dolar symbol
const html = `
  <div>
    ${Math.random()}
  </div>
`;

html